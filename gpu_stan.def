BootStrap: docker
From: mattocci/cmdstanr
Registry: 210.72.93.96:5000

%apprun R
  exec R "${@}"

%apprun Rscript
  exec Rscript "${@}"

%runscript
  exec R "${@}"

%post
    DEBIAN_FRONTEND=noninteractive
    chmod -R 777 /opt/cmdstan